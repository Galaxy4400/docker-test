services:
  date:
    image: galaxy4400/result-date
    build:
      dockerfile: date.dockerfile
  result:
    image: galaxy4400/result
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - date
